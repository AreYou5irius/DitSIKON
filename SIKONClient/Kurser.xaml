<Page
    x:Class="SIKONClient.Kurser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SIKONClient"
    xmlns:library="using:SIKONClassLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <local:ViewModel/>
    </Page.DataContext>

    <Page.Resources>
        <DataTemplate x:Key="KurserTP" x:DataType="library:Event">
            <Grid>
               <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
               </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Category}" Grid.Column="1" Grid.Row="0"></TextBlock>
                <TextBlock Text="{Binding Subject}" Grid.Column="1" Grid.Row="1"></TextBlock>
            </Grid>
        </DataTemplate>
    </Page.Resources>
  
    <Grid>
        <TextBlock Text="Dette er en liste"></TextBlock>
        <ListView ItemsSource="{Binding KursusListe}" ItemTemplate="{StaticResource KurserTP}">
            
        </ListView>

    </Grid>
</Page>
